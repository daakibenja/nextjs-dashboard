export default function PackagePricing({ pricing }: { pricing: string }) {
    return <div>

        <p className="text-[#00153B] text-[50px] leading-[63px] font-bold">
            {pricing}
        </p>
    </div>
}